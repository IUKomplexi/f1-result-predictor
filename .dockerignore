# Python tooling / build artifacts (rebuilt in-image)
.venv/
__pycache__/
*.py[cod]
.pytest_cache/
build/
*.egg-info/
ruff_errors.txt
pyright_errors.json

# Frontend deps + build output (built in the ui stage)
f1web/ui/node_modules/
f1web/ui/dist/

# Not needed in the build context (runtime image only gets what the
# Dockerfile explicitly copies; README.md stays — setuptools reads it).
tests/
docs/
f1weather/

# Git + tooling state
.git/
.github/
.reasonix/
.vscode/
