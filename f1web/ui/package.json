{
  "name": "f1-dashboard",
  "private": true,
  "version": "0.1.0",
  "description": "Browser dashboard for the F1 result predictor (consumes the internal /api).",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc -b && vite build",
    "lint": "oxlint",
    "preview": "vite preview"
  },
  "dependencies": {
    "preact": "^10.26.4"
  },
  "devDependencies": {
    "@preact/preset-vite": "^2.9.6",
    "@types/node": "^22.10.2",
    "@types/react": "^18.3.12",
    "@types/react-dom": "^18.3.1",
    "oxlint": "^0.15.0",
    "typescript": "~5.5.4",
    "vite": "^5.4.11"
  }
}
